@* GENERATED by admin-kits/data/generate.mjs from admin-kits/shared. Do not edit. *@
@implements IDisposable
@inject NavigationManager Nav

<nav class="adminnav" aria-label="Console">
    <div class="adminnav__head"><span class="theme-novapay vlogo"><svg class="vmark" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3.5" y="6" width="17" height="12" rx="2"/><line x1="3.5" y1="9.5" x2="20.5" y2="9.5"/><rect x="6" y="12" width="3.6" height="3" rx="0.6"/></svg><span class="wm"><span class="nm">nova</span><span class="sf">pay</span></span></span><label for="navtoggle" class="btn btn--ghost btn--sm toggle44" title="Close menu"><Icon Name="close" /></label></div>
    <NavLink class="navlink" href="" title="Dashboard" Match="NavLinkMatch.All"><Icon Name="dashboard" /><span>Dashboard</span></NavLink>
    <NavLink class="navlink" href="analytics" title="Analytics"><Icon Name="chart" /><span>Analytics</span></NavLink>
    <details class="navgroup" open="@(InGroup("transactions", "datagrid", "terminals"))">
        <summary class="navgroup__label"><Icon Name="layers" Class="icon--sm" /><span>Operations</span><Icon Name="chevron-down" Class="icon--sm navgroup__chevron" /></summary>
        <NavLink class="navlink" href="transactions" title="Transactions"><Icon Name="transactions" /><span>Transactions</span></NavLink>
        <NavLink class="navlink" href="datagrid" title="Data grid"><Icon Name="grid" /><span>Data grid</span></NavLink>
        <NavLink class="navlink" href="terminals" title="Terminals"><Icon Name="terminal" /><span>Terminals</span></NavLink>
    </details>
    <details class="navgroup" open="@(InGroup("settings"))">
        <summary class="navgroup__label"><Icon Name="settings" Class="icon--sm" /><span>Configuration</span><Icon Name="chevron-down" Class="icon--sm navgroup__chevron" /></summary>
        <NavLink class="navlink" href="settings" title="Settings"><Icon Name="settings" /><span>Settings</span></NavLink>
    </details>
</nav>

@code {
    bool InGroup(params string[] routes)
    {
        var path = Nav.ToBaseRelativePath(Nav.Uri).Split('?', '#')[0].Trim('/');
        return routes.Contains(path);
    }

    protected override void OnInitialized() => Nav.LocationChanged += Changed;
    void Changed(object? sender, LocationChangedEventArgs e) => InvokeAsync(StateHasChanged);
    public void Dispose() => Nav.LocationChanged -= Changed;
}
